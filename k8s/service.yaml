apiVersion: v1
kind: Service
metadata:
  name: python-app-service
  namespace: python-app

spec:
  type: NodePort

  selector:
    app: python-app

  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30007

